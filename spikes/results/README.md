Phase 0 evidence notes belong here.

Do not commit machine-specific dumps, plugin logs, or files that may contain account data.

Sanitized notes may be added as markdown after a real-device run. Historical Python harnesses for those runs are in `spikes/python/` and are not shipped with the plugin.
