Historical Phase 0 and later device harnesses. They are not part of the shipped plugin and are not kept in sync with the 1.0 backend.

Evidence notes for those runs are in `spikes/results/`.

`multi_remote_spike.py` is the v1.1.0 CEF-icon / appstreamcli-env / Flathub-filter proof. It does not mutate remotes or installs.

